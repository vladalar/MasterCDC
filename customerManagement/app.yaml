runtime: php74 # Replace with php74 to use PHP 7.4

runtime_config:
  document_root: .

handlers:
# Serve a directory as a static resource.
- url: /stylesheets
  static_dir: stylesheets


# Serve images as static resources.
- url: /(.+\.(gif|png|jpg))$
  static_files: \1
  upload: .+\.(gif|png|jpg)$

# Serve your app through a front controller at index.php or public/index.php.
- url: .*
  script: auto

entrypoint:
  handler.php

env_variables:
  CLOUDSQL_USER: root
  CLOUDSQL_DB: customermngmt_db
  CLOUDSQL_PASSWORD: test
  CLOUDSQL_DSN: /cloudsql/customermanagement-313217:europe-central2:customermngmt